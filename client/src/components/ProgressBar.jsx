import React from 'react'

function ProgressBar({type, info}) {


        const percentage = (info.completed/info.total)*100
   
        

    return (
        <div className='progressBar '>
            
        </div>
    )
}

export default ProgressBar
